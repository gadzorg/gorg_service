require 'spec_helper'

describe Gorg::RabbitmqBot do
  it 'has a version number' do
    expect(Gorg::RabbitmqBot::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
